import { IoMdMenu } from 'react-icons/io';

const MenuButton = () => {
  return (
    <button className='h-full py-3'>
      <IoMdMenu className='h-full w-full' />
    </button>
  );
};

export default MenuButton;
